#include "assets/texture.h"

Texture *textureNew(TextureFormat format, uint8_t *data, unsigned width, unsigned height, unsigned mipmaps)
{

}

void textureFree(Texture *texture)
{
	assert(texture != NULL);
}